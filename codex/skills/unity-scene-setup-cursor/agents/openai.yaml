interface:
  display_name: "Unity Scene Setup"
  short_description: "Scene and prefab workflow bridge"
  default_prompt: "Use $unity-scene-setup-cursor to plan and validate Unity scene or prefab changes."

policy:
  allow_implicit_invocation: true
