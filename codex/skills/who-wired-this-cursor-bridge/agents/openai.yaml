interface:
  display_name: "Who Wired This Bridge"
  short_description: "Loads Cursor project rules for Codex"
  default_prompt: "Use $who-wired-this-cursor-bridge to follow this repo's Cursor rules while working in Codex."

policy:
  allow_implicit_invocation: true
